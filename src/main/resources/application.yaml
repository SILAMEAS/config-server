server:
  port: ${PORT_CONFIG_SERVER}
spring:
  profiles:
    active: native
  application:
    name: server-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configuration



